{
  "name": "obsidian-fathom-sync-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that receives Fathom webhooks and exposes a polling endpoint for the Obsidian Fathom Sync plugin.",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240502.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.50.0"
  }
}
